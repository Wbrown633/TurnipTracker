This is a simple discord bot written using the python API to track the turnip
prices of multiple users in a server.

The following functionality is supported in `v0.3`

- `$turnip (price) [AM/PM] [MM/DD/YY]`
